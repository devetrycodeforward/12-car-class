# Car class
Class Car:
    # constructor function
    def __init__(self, gas_level):
        self.gas = float(gas_level)
        return self.gas
    #fill_up function
    def fill_up(gas_up)
        if self.gas + float(gas_up) >= 13:
            return 0
        else:
            return 13 - self.gas
    #add_gas function

